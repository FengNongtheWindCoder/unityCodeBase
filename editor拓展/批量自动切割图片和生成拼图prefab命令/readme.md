脚本放置于editor下可以使用。  
可以进行图片的n*m等分切割生成sprite，功能与sprite editor操作结果一样，支持一次选中多个图。  
可以利用生成的全部碎片sprite组合一个prefab，每个sprite位置与在原图位置相同，也就是这个prefab放到场景中直接就显示为一张由各sprite组成的完整图片。    
提供了4*4切割，4*4切割同时生成prefab，利用现有sprite生成prefab三个菜单。  
看不懂直接拖个图进去，点一下试试就懂了。  